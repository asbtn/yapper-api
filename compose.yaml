services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "3000:3000"
    volumes:
      - .:/rails
    environment:
      - RAILS_ENV=development
  db:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: postgres
